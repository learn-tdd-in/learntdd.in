#!/usr/bin/env ruby

require 'dotenv'
Dotenv.load

puts 'ember'
system("bin/log2blog generate learn-tdd-in/ember 1137f60 --token #{ENV['GITHUB_OAUTH_TOKEN']} --skip \"tests/unit\" > ember/_commits.md")

puts 'rails'
system("bin/log2blog generate learn-tdd-in/rails a5cea8f --token #{ENV['GITHUB_OAUTH_TOKEN']} --skip db/schema.rb > rails/_commits.md")

puts 'cocoa-touch'
system("bin/log2blog generate learn-tdd-in/cocoa-touch ae3a817 --token #{ENV['GITHUB_OAUTH_TOKEN']} --skip \"Main\.storyboard|project\.pbxproj\" > cocoa-touch/_commits.md")
